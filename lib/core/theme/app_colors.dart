import 'package:flutter/material.dart';

class AppColors {
  // Brand colors
  static const Color primary = Color(0xFF1E56A0); // Professional Deep Royal Blue
  static const Color primaryLight = Color(0xFF2E75D6);
  static const Color primaryDark = Color(0xFF123B70);

  // Print button action color (High contrast, instantly visible)
  static const Color printAction = Color(0xFF0E8A16); // High-confidence green
  static const Color printActionHover = Color(0xFF0B6F12);

  // Sidebar colors
  static const Color sidebarBg = Color(0xFF151922);
  static const Color sidebarItemHover = Color(0xFF222836);
  static const Color sidebarItemSelected = Color(0xFF1E3A5F);
  static const Color sidebarText = Color(0xFFD3D8E2);
  static const Color sidebarTextMuted = Color(0xFF8E99AB);

  // Workspace canvas colors
  static const Color workspaceBg = Color(0xFFEEF2F6);
  static const Color canvasSheetBg = Color(0xFFFFFFFF);
  static const Color canvasPaperBorder = Color(0xFFB0BCCB);
  static const Color canvasGridLine = Color(0xFFE2E8F0);
  static const Color canvasRulerBg = Color(0xFFDFE6EE);
  static const Color canvasRulerText = Color(0xFF475569);

  // Card / Surface colors
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceLight = Color(0xFFF8FAFC);
  static const Color border = Color(0xFFD9E2EC);
  static const Color borderStrong = Color(0xFFBCCCDC);

  // Typography
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF475569);
  static const Color textMuted = Color(0xFF94A3B8);

  // Status & Badges
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF3B82F6);

  // Service specific accents
  static const Color aadhaarAccent = Color(0xFFD97706); // Orange / Saffron
  static const Color passportAccent = Color(0xFF059669); // Emerald
  static const Color panAccent = Color(0xFF0284C7); // Sky Blue
  static const Color docAccent = Color(0xFF7C3AED); // Purple
}
